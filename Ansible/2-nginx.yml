- name: Install nginx and start service
  hosts: warrior
  become: yes

  tasks:
  - name: insatll nginx
    ansible.builtin.package:
    name: nginx
    state: present
  
  - name: start nginx
    ansible.builtin.service:
    name: nginx
    state: started
    enabled: yes

